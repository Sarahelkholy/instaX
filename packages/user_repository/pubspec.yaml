name: user_repository
description: dart package manages user

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  cloud_firestore: ^6.0.2
  cupertino_icons: ^1.0.6
  equatable: ^2.0.7
  firebase_auth: ^6.1.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
